{
  "name": "bridgeworld-stats",
  "version": "1.0.0",
  "description": "Subgraph that powers Bridgeworld Statistics",
  "license": "MIT",
  "scripts": {
    "build:dev": "graph build",
    "build:staging": "true",
    "build:prod": "graph build",
    "codegen:dev": "graph codegen",
    "codegen:staging": "true",
    "codegen:prod": "graph codegen",
    "deploy:dev": "graph deploy --product hosted-service treasureproject/bridgeworld-stats-dev",
    "deploy:staging": "true",
    "deploy:prod": "graph deploy --product hosted-service treasureproject/bridgeworld-stats",
    "prepare:dev": "mustache ../../node_modules/@treasure/subgraph-config/src/arbitrum.json template.yaml > subgraph.yaml",
    "prepare:staging": "true",
    "prepare:prod": "mustache ../../node_modules/@treasure/subgraph-config/src/arbitrum-testnet.json template.yaml > subgraph.yaml",
    "test": "graph test"
  }
}
