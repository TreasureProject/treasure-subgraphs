{
  "name": "@treasureproject/treasure-subgraphs",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "subgraphs/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "codegen": "yarn workspaces run codegen",
    "deploy": "yarn workspaces run deploy",
    "prepare:arbitrum": "yarn workspaces run prepare:arbitrum",
    "prepare:arbitrum-testnet": "yarn workspaces run prepare:arbitrum-testnet",
    "prepare:mainnet": "yarn workspaces run prepare:mainnet",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1",
    "matchstick-as": "0.2.3",
    "mustache": "4.2.0",
    "typescript": "4.5.5"
  },
  "resolutions": {
    "colors": "1.4.0"
  }
}
