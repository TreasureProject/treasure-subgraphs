name: Deploy Graph
on:
  workflow_dispatch:
    inputs:
      environment:
        type: environment
        description: Select environment
      subgraph:
        type: choice
        description: Select subgraph
        options:
          - bridgeworld
          - bridgeworld-approvals
          - bridgeworld-corruption
          - bridgeworld-kote
          - bridgeworld-recruits
          - magicswap-exchange
          - smol
      version:
        description: Subgraph version
        required: true
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./subgraphs/${{ inputs.subgraph }}
    steps:
      - name: Check out code
        uses: actions/checkout@v4
      - name: Install Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install Goldsky
        run: curl -fsSL https://cli.goldsky.com/install | bash
      - name: Install dependencies
        run: npm ci
      - name: Prepare for DEV
        if: inputs.environment == 'Development'
        run: npm run prepare:dev --if-present
      - name: Prepare for PROD
        if: inputs.environment == 'Production'
        run: npm run prepare:prod --if-present
      - name: Generate code
        run: npm run codegen --if-present
      - name: Test
        run: npm test --if-present
      - name: Deploy to DEV
        if: inputs.environment == 'Development'
        run: goldsky subgraph deploy ${{ inputs.subgraph }}-dev/${{ inputs.version }} --token ${{ secrets.GOLDSKY_DEPLOY_KEY }}
      - name: Deploy to PROD
        if: inputs.environment == 'Production'
        run: goldsky subgraph deploy ${{ inputs.subgraph }}/${{ inputs.version }} --token ${{ secrets.GOLDSKY_DEPLOY_KEY }}
